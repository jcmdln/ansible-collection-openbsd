# SPDX-License-Identifier: ISC
---
- name: Configure ntpd
  ansible.builtin.template:
    src: etc/ntpd.conf.j2
    dest: /etc/ntpd.conf
    owner: root
    group: wheel
    mode: 0600
  notify:
    - Enable ntpd
    - Start ntpd
