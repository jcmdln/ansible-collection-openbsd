---
# inventory.yml

all:
  vars:
    ansible_become_method: doas
    ansible_become_user: root
    ansible_connection: ssh
    ansible_python_interpreter: /usr/local/bin/python3
    ansible_user: root

servers:
  hosts:
    openbsd.local:
      ansible_host: 192.168.1.210
      server_domain: openbsd.local
